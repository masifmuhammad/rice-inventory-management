# Dev server port (npm start) — UI only. API lives on Docker :5000.
PORT=3005

# Call the API directly (bypass CRA proxy). Restart `npm start` after changing this.
REACT_APP_API_URL=http://127.0.0.1:5000/api
